# Local collector secrets (never commit this file)

# Copy to ~/.config/ai-usage-dashboard/env and fill in.
# scripts/local-snapshot.sh sources it on every 15-minute tick.

OPENROUTER_API_KEY=
OPENROUTER_MANAGEMENT_KEY=
SAIL_API_KEY=
